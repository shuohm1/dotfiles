# .colordiffrc

# show a splash screen: yes/no
banner=no

# set colors even if output is redirected: yes/no
color_patches=no

# a diff command
diff_cmd=diff

# `git-diff`-like color settings
plain=off
oldtext=darkred
newtext=darkgreen
diffstuff=darkcyan
cvsstuff=white

# * available colors:
#   black, red, green, yellow, blue, magenta, cyan, white,
#   darkblack, darkred, darkgreen, darkyellow,
#   darkblue, darkmagenta, darkcyan, darkwhite
# * no color (any of these are the same):
#   none, normal, off
